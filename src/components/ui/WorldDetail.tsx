'use client';

import React from 'react';
import { DeepExplorationPanel } from './DeepExplorationPanel';

const WorldDetail: React.FC = () => {
  return <DeepExplorationPanel />;
};

export default React.memo(WorldDetail);
